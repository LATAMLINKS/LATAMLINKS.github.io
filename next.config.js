/** @type {import('next').NextConfig} */
/* uncomment for development 
 * const nextConfig = {}
 * module.exports = nextConfig
 */

/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    output: 'export',
}

module.exports = nextConfig